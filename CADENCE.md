# 升级节奏 Playbook (CADENCE.md)

> 架构级规则：**每 12 小时一次产品升级，每 24 小时一次网站设计升级**。
> 本文件是每次定时运行的**确定性脚本**。维护者：主控 Claude。

## 0. 为什么是 Routines（而非会话内计时器）
本项目跑在 Claude Code on the web 的**临时容器**里——会话结束即回收，无法在会话内挂一个跨天的计时器。持久的定时机制是 **Routines**（claude.ai/code/routines）：到点开一个**全新云端 session**，从默认分支克隆仓库并执行下面的循环。
> 因此每次运行都是"失忆"的新会话——**仓库（本文件 + MEMORY.md + BRAND.md）是唯一的跨运行记忆**。每轮必须"先读 backlog、后写回进度"。

## 1. 两条节奏
| 节奏 | 周期 | 入口 | 领衔 | 调度配置 |
|------|------|------|------|----------|
| 产品升级 | 每 12h | `/product-upgrade`（见 `.claude/commands/`） | 浩哥（实现 agent 落地） | daily×2，相隔 12h（如 08:00 / 20:00）；有 CLI/Desktop 则 cron `0 */12 * * *` |
| 设计升级 | 每 24h | `/design-upgrade` | Sheng 提案 → 龙哥审查 → 实现 agent 落地 | daily 预设（如 03:00，错峰）；cron `0 3 * * *` |

## 2. 每轮通用流程（两条节奏都遵守）
1. **读状态**：读 `CADENCE.md`（本文件）的 backlog、`MEMORY.md` §待办、`BRAND.md`（产品）或 Sheng 设计 roadmap（设计），确定"本轮做哪一件"。**一轮只做一件、改动有界**。
2. **执行**：实现该增量（产品=功能/工具/取数；设计=下一阶段视觉）。
3. **质量门禁（硬性，不过不合并）**：`npm run typecheck` + `npm test` + `npm run lint`（前端）；改后端则 `cd backend && .venv/bin/python -m pytest`。
4. **安全判定**（见 §3）：在"安全范围"内且门禁全绿 → 开 PR 到默认分支并合并上线；否则 → 开 PR、**不合并**、在 PR 里写清原因等人/投票。
5. **写回状态**：在本文件 backlog 勾掉已完成项、追加"下一轮建议"；更新 `MEMORY.md` §待办与 PR 记录。**这是跨运行连续性的关键，绝不能跳过。**
6. **一句话产出**：运行结束在 PR 描述里写：做了什么 / 门禁结果 / 是否已上线 / 下一轮建议。

## 3. 安全护栏（自动上线 vs 停下等人）
**✅ 可自动合并上线（安全范围，且门禁全绿、diff 有界）：**
附加式 UI/UX 打磨、文案、新的非破坏性组件、内容（城市/国旗/图片）、性能/缓存、工具与取数、测试、无障碍、**已在 roadmap 内**的设计 token 迭代。

**🛑 必须停在 PR、不自动合并（需老板/投票）：**
- 品牌名/品牌识别变更
- 价格逻辑、真实交易/支付/预订集成的行为变更
- 数据库结构（必须先改 `backend/schema.sql`）、新增基础设施（真库、消息队列）
- 删除/覆盖既有面向用户的功能；依赖大版本升级；改 secrets/env
- 任何不可逆或法律敏感项（FIFA/World Cup 商标、隐私）

**永远：** 门禁失败 → 不合并，开 PR 写诊断；改动超出"一件事"的边界 → 拆分，多余的写进 backlog 下一轮做。

## 4. 角色
- **产品升级（12h）**：浩哥定方向（迭代/工具/取数/DB/MCP/队列的*评估与落地*），实现 agent 写码。涉及 §3 的 🛑 项 → 走投票而非自动上线。
- **设计升级（24h）**：Sheng 出方案（先给"情绪锚点"再给像素，龙哥 KPI 要求）→ 龙哥审查并打**审美分/沟通效率分**→ 实现 agent 落地。

## 5. 产品 Backlog（每轮从顶部取一件，做完勾掉）
来源：BRAND.md 的 P0/P1。
- [x] **全程总价汇总（第一步）** —— 纯前端附加式，分项显示机票/酒店/交通/赛票各类成本（浩哥交付，commit `2d795a9`）
- [ ] 接入**真实 2026 世界杯赛程数据**（替换/补充 seed），含来源与缓存策略（浩哥评估联网取数）
- [ ] 多城市"追随球队"行程引擎（P0 护城河）
- [ ] 逐段签证/入境提示（美 ESTA / 加 eTA / 墨 FMM）
- [ ] 全程总价汇总 + 多币种（USD/CAD/MXN）—— 后续可扩展为多币种视图
- [ ] 价格/降价提醒（拉升可佣金成交）
- [ ] 联盟佣金追踪（北极星：每个已规划行程的可佣金成交数）

**下一轮建议：**
- 落地「真实赛程数据缓存」（前置于多城市引擎），构建取数→缓存→展示三层；评估 Redis 或 LRU 缓存成本。
- 接着做视觉升级（配色/阴影）并联动成本分项的高级美化（金色价格、稀缺徽章）。

## 6. 设计 Roadmap（每轮推进一步）
来源：Sheng 报告（见 MEMORY.md §5）。
- [ ] **情绪基调参考板**（3 张非竞品图定义产品气质）——龙哥 KPI 前置要求，先做
- [x] 骨架屏加载态（shimmer） —— 完成（commit `111f5c5`）
- [ ] 完整 8px 间距系统（`--sp-*` token 化）
- [ ] 价格对比徽章（原价/折扣/稀缺）——上线走 A/B
- [ ] 圆角 12→16/20、排版字阶精细化、卡片彩色阴影

## 7. 如何创建这两个 Routine（网页 UI，一次性）
在 **claude.ai/code/routines → New routine**：
1. **产品升级**：Prompt 填 `执行本仓库 CADENCE.md 的「12h 产品升级」循环（入口 /product-upgrade）`；仓库选本仓库；环境按需放开网络（取真实数据/价格时把对应域名加进 Allowed domains，并配 DUFFEL_API_KEY / LITEAPI_API_KEY）；触发器加 **两个 daily**（08:00、20:00）。
2. **设计升级**：Prompt 填 `执行本仓库 CADENCE.md 的「24h 设计升级」循环（入口 /design-upgrade）`；触发器 **daily**（如 03:00 错峰）。
3. 分支：默认只允许推 `claude/` 前缀分支——我们的部署/功能分支都是该前缀，无需放开"unrestricted branch pushes"。
4. 注意 Routines **每日有运行次数上限**且消耗订阅额度；龙哥/Sheng/浩哥等子 agent 在云端 session 内照常工作。

> 改这套节奏/护栏的权力属于老板（架构与规则管理）。agent 只在范围内执行。
