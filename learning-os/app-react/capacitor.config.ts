import type { CapacitorConfig } from "@capacitor/cli";

// Native shell config. appId/appName are placeholders — change before submitting to the stores.
const config: CapacitorConfig = {
  appId: "com.moneyzoo.app",
  appName: "Money Zoo",
  webDir: "dist",
  backgroundColor: "#ecfeff",
};

export default config;
