# CLAUDE.md

> 项目记忆文件。后续 build 会话**先读这里**。完整方案见 [`PRODUCT_PLAN.md`](./PRODUCT_PLAN.md)。

## 这是什么

面向**安防 / 弱电 / AV 集成商**的**多租户 SaaS**：客户接入自己的供应商账号，软件自动**跨供应商比价 → 按客户策略定价 → 一键出报价单**。攻击两大痛点：① 供应商无开放 API、价格藏在登录态后；② 报价/采购流程全人工、沟通成本高。

## 当前状态

**方案讨论阶段，尚无代码、无构建命令、技术栈未定。** 下一步从 MVP 两块核心起：**产品归一化** + **ADI 连接器**。

## 不可违背的决策（build 时必须遵守）

1. **卖软件，不做转售。** 多租户 SaaS，每个客户连他们自己的供应商账号。
2. **多租户 + 每租户独立凭证保险库，从第一天就架进去。** 信任地基，不是后期功能：凭证加密、隔离、最小授权、全程审计。
3. **边界干净**：创始人个人公司只是 dogfood 试验场；**不使用雇主的账号 / 数据 / 历史报价**。测试用**合成但真实的 BOM**。
4. **定价 human-in-the-loop**：动钱步骤一律人工拍板；记录人工覆盖用于学习。
5. **价格标注时效 / 置信度**；不可每次报价都现爬 → 需缓存 + 刷新窗口。

## MVP 范围（Phase 1：比价 + 出报价）

- 供应商：**ADI（主，认证态浏览器 agent，无 2FA）** + **Amazon Business（commodity 线缆/配件）**。
- 闭环：BOM → 归一化/匹配 → 抓价 → 选最便宜源 + 定价(成本×加价，可拖动) → 出独立报价单(PDF/CSV) → 记录。
- **不在范围**：Ingram（官方 API 属雇主，出局）、Halo 集成、自然语言→BOM、下单/库存/项目/工单。

## 架构（详见 PRODUCT_PLAN.md §7–8）

连接器层 → 归一化层（产品身份图谱）→ 报价引擎 →（后续）采购编排 → Agent/对话层 → PSA 集成层。
横切：**凭证保险库**、**价格缓存**。

## 护城河

连接器库 + 跨供应商产品身份图谱 + 凭证安全。**不是"套了个大模型"。**

## 技术栈

**未定（TBD）。** 选型时优先支持：多租户、安全凭证管理、浏览器自动化（ADI 连接器）、LLM 调用（产品归一化）。

## 开放问题（阻塞推进，见 PRODUCT_PLAN.md §15）

- 个人 LLC 是否已注册？
- 第一个 ADI 测试账号：自己开 vs 找设计伙伴？
- ADI 是否有 CAPTCHA / 限流？
- 最常报、≥2 家都能买的 2–3 个重叠品类？

## 约定

- 与用户沟通用**中文**。
- 提交信息清晰描述改动。
- 商业模式：卖软件订阅，**不抽采购流水百分比**。
